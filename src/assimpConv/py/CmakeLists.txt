# Add blender py scripts as well
file(GLOB_RECURSE BLENDER_SCRIPTS *.py)
add_custom_target(AssimpConverterPy ${BLENDER_SCRIPTS})
#copy_static_files(AssimpConverter "tools/py" "${BLENDER_SCRIPTS}")
copy_static_files(AssimpConverterPy "tools/py" "${BLENDER_SCRIPTS}")
set_target_properties(AssimpConverterPy PROPERTIES FOLDER tools)