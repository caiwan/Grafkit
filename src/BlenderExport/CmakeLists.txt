# @echo off
# blender --debug-memory -b -P exporter.py -- --input testdata/untitled.blend --output output/untitled.scene 

file(GLOB_RECURSE FILES *.*)
file(GLOB_RECURSE PY_FILES *.py)

add_custom_target(BlenderExport SOURCES ${FILES})
set_target_properties(BlenderExport PROPERTIES FOLDER tools)

foreach(py_file arg1 arg2 ...)
  
endforeach(loop_var)

#install(FILES BlenderExport "${PY_FILES}" DESTINATION tools)

assign_source_group("${FILES}")
