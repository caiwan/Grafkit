include_directories(${GTEST_INCLUDE_DIRS})

file(GLOB_RECURSE SOURCE_FILES *.cpp)
file(GLOB_RECURSE HEADER_FILES *.h)

include_directories("${CMAKE_SOURCE_DIR}")
include_directories("${GK2_INCLUDE_DIR}")
include_directories("${GTEST_INCLUDE_DIRS}")

enable_testing()
add_executable(BlenderImporterIT EXCLUDE_FROM_ALL ${SOURCE_FILES} ${HEADER_FILES})
add_test(Tests BlenderImporterIT)

target_link_libraries(BlenderImporterTest Grafkit2 ${GTEST_BOTH_LIBRARIES})
target_include_directories(BlenderImporterTest PUBLIC include)

set_target_properties(BlenderImporterTest PROPERTIES FOLDER tests)
