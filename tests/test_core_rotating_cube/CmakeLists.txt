
# -----------------------------------------------------------------------

file(GLOB_RECURSE SOURCE_FILES *.cpp)
file(GLOB_RECURSE HEADER_FILES *.h)

include_directories("${CMAKE_SOURCE_DIR}")

add_executable(test_core_rotating_cube ${SOURCE_FILES} ${HEADER_FILES})

target_link_libraries (test_core_rotating_cube Grafkit2)
target_include_directories (test_core_rotating_cube PUBLIC include)

set_target_properties(test_core_rotating_cube PROPERTIES FOLDER tests)

assign_source_group("${SOURCE_FILES}")
assign_source_group("${HEADER_FILES}")

# -----------------------------------------------------------------------